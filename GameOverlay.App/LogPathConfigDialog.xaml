<Window x:Class="GameOverlay.App.LogPathConfigDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        mc:Ignorable="d"
        d:DesignHeight="280" d:DesignWidth="550"
        Title="Configurer le chemin du fichier log" Height="280" Width="550"
        WindowStartupLocation="CenterScreen" 
        ResizeMode="NoResize" 
        Topmost="True"
        ShowInTaskbar="False"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Border Background="#80000000" BorderBrush="{DynamicResource CyanAccentBrush}" BorderThickness="2" CornerRadius="5" MouseLeftButtonDown="Border_MouseLeftButtonDown">
        <Grid x:Name="MainGrid" Margin="20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Titre et bouton fermer -->
            <Grid Grid.Row="0" Margin="0,0,0,15">
                <TextBlock Text="{Binding Title}" 
                           FontSize="14" 
                           FontWeight="Bold"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"/>
                <Button Content="✕" 
                        Width="25" 
                        Height="25"
                        Click="CloseButton_Click"
                        Background="Transparent"
                        Foreground="{DynamicResource CyanAccentBrush}"
                        BorderBrush="{DynamicResource CyanAccentBrush}"
                        BorderThickness="1"
                        FontWeight="Bold"
                        FontSize="16"
                        Padding="0"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Top"
                        Cursor="Hand"/>
            </Grid>

            <TextBlock Grid.Row="1" 
                       Text="Chemin du fichier:" 
                       FontSize="12"
                       Margin="0,0,0,8"/>

            <Grid Grid.Row="2" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <TextBox x:Name="PathTextBox" 
                         Grid.Column="0"
                         Height="35"
                         VerticalContentAlignment="Center"
                         Margin="0,0,10,0"/>
                
                <Button Grid.Column="1"
                        Content="📁 Parcourir" 
                        Width="100"
                        Height="35"
                        Click="BrowseButton_Click"
                        Padding="5,0"/>
            </Grid>

            <TextBlock Grid.Row="3" 
                       Text="Laissez vide pour utiliser la détection automatique" 
                       FontSize="10"
                       FontStyle="Italic"
                       Foreground="#88FFFFFF"
                       Margin="0,0,0,15"
                       TextWrapping="Wrap"/>

            <StackPanel Grid.Row="4" 
                        Orientation="Horizontal" 
                        HorizontalAlignment="Right" 
                        Margin="0,10,0,0">
                <Button x:Name="CancelButton" 
                        Content="Annuler" 
                        Width="90"
                        Height="40"
                        Click="CancelButton_Click"
                        Margin="0,0,15,0"/>
                
                <Button x:Name="OkButton" 
                        Content="Valider" 
                        Width="90"
                        Height="40"
                        Click="OkButton_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>






